language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t aritid/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true aritid/docker-react npm run test -- -- coverage